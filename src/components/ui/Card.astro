---
interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<div class={`bg-white rounded-lg border border-blue-100 p-6 ${className || ''}`}>
  <slot />
</div>

<style is:global>
  /* Aquí puedes añadir estilos globales si es necesario para las tarjetas */
</style>